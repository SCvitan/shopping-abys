To run the spring boot app use the maven wraper command from the folder with pom.xml:

 - ./mvnw spring-boot:run

Or build the jar file:
  - mvn clean install

And run the jar file:
   - java -jar target/*.jar
